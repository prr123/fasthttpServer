# fasthttp server
adopted vayalla fast http server.

## upgrader
upgrades the server connection from http to websocket.

## fasthttps
implementation of fasthttp server using tls for connection.

## fasthttpsmap
implements a router based on a map rather than the switch statement.
